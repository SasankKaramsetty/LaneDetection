# JDBC connection configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate & JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=tru

# Optional: MySQL dialect selection (auto-detected; no need to set explicitly)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Suppress JTA warning (if not using JTA)
spring.jpa.properties.hibernate.transaction.coordinator_class=jdbc

# Server configuration
server.port=8081
twilio.accountSid=AC492c73e92aed92095ba69ac223b6476c
twilio.authToken=4c7ba255fd16a9572364c6c0ea961f10
twilio.trial_number=+918328596959
